Synchronize translation files across all locales.

Compare `messages/en.json` against `messages/fr.json`, `messages/zh.json`, and `messages/mi.json`. Identify missing keys and generate translations for them.
